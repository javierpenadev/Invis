# Тасклист: Порт InviZible Pro → Windows (Electron)

Дополнение к `ТЗ-порт-Windows.md`. Оценки — в идеальных человеко-днях (ИЧД) для одного
разработчика, знакомого с Node/Electron. Приоритет: **P0** — без этого релиза нет,
P1 — важно, P2 — можно отложить. Каждая задача имеет DoD (definition of done).

Каркас проекта берём из `example/` (уже очищен: тайтлбар, контрол-бар, панели,
лог-консоль, прогресс, IPC-мост).

---

## Фаза 0 — Фундамент (≈2 ИЧД)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 0.1 | Создать репозиторий проекта из `example/`: новое имя продукта (придумать, не InviZible), package.json, .gitignore, git init | `npm start` открывает окно шаблона; git-история начата | P0 |
| 0.2 | Каркас IPC: файл `ipc-api.js` в main (реестр каналов), в рендерере обёртка `Api.invoke()` | Все каналы через один реестр; шаблон использует Api вместо прямых require | P0 |
| 0.3 | StoreService: JSON-настройки в `userData` (+ режим portable рядом с exe), схема дефолтов, миграции версий | Настройки переживают рестарт; повреждённый файл восстанавливается из дефолтов | P0 |
| 0.4 | LogService: 3 канала + общий, кольцевой буфер, события в рендерер, экспорт в файл | Логи видны в UI, экспорт работает | P1 |
| 0.5 | i18n-каркас (ru/en), словари вынесены | Все строки шаблона через словарь | P1 |

## Фаза 1 — Демоны (сердце порта, ≈5 ИЧД)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 1.1 | Скачать и положить официальные Windows-сборки: tor (Expert Bundle), dnscrypt-proxy, i2pd; скрипт `tools/fetch-bins` с фиксацией версий и хэшей | `resources/bin/…` (dev: `bin/…`) заполнен; версии зафиксированы | P0 |
| 1.2 | DaemonSupervisor: spawn/stop/restart tor, dnscrypt, i2pd; backoff-рестарт; pid-файлы; очистка «зомби» (taskkill /T) при старте и выходе | Старт/стоп каждого модуля из UI; после убийства UI не остаётся процессов | P0 |
| 1.3 | Парсеры логов: Tor bootstrap (0–100%, статусы), dnscrypt ready/errors, i2pd ready | В UI виден осмысленный статус, а не сырой поток | P0 |
| 1.4 | Звуки готовности (mp3 из оригинального APK — заменить на свои) + уведомления Windows | «Модуль готов» — звук + toast | P2 |
| 1.5 | Tray: иконка, статусы модулей, старт/стоп, «Сменить IP», выход | Полное управление из tray; single instance | P1 |

## Фаза 2 — Конфиги и настройки (≈5 ИЧД)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 2.1 | ConfigService: генератор `torrc` (SocksPort 9050, ControlPort 9051 + авто-hash пароля, DNSPort 5400, TransPort 9040, NoExec, DataDirectory, bridges) | Tor стартует с сгенерированного конфига; порт 9050 отвечает SOCKS | P0 |
| 2.2 | Генератор `dnscrypt-proxy.toml` (listen, серверы, sources с minisign, require_*, cache, force_tcp, файлы правил) | dnscrypt стартует, `nslookup` через 127.0.0.1:9053 резолвит | P0 |
| 2.3 | Генераторы `i2pd.conf` + `tunnels.conf` (HTTP 4444, HTTPS 4447, консоль 7070, bandwidth, NTCP2/SSU2, reseed) | Консоль 7070 открывается; прокси 4444 работает | P1 |
| 2.4 | Экран «Настройки → Tor»: мосты (obfs4/snowflake amazon/amp/cdn77/свой STUN/conjure/свои строки), узлы, изоляция, продвинутый редактор torrc | Изменение настроек → перегенерация конфига → мягкий рестарт модуля | P1 |
| 2.5 | Экран «Настройки → DNSCrypt»: выбор серверов (категории из ТЗ §2.2), источники, фильтры | Выбор «только nolog» отражается в toml | P1 |
| 2.6 | Экран «Настройки → I2Pd» + продвинутый редактор i2pd.conf/tunnels.conf | Изменение bandwidth применяется после рестарта | P2 |
| 2.7 | Экран Dashboard: карточки модулей, статусы, кнопки ▶/⏹/Сменить IP (ControlPort NEWNYM), прямая/Tor-версия внешнего IP | Кнопки работают из одного окна; статусы обновляются live | P0 |
| 2.8 | Экран «Логи» (области A/B шаблона): 3 канала, фильтр, пауза, экспорт | Совпадает с DoD 0.4, но на финальном UI | P1 |

## Фаза 3 — Сетевые режимы P1/P2 (≈4 ИЧД, нужен админ-тестовый стенд)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 3.1 | NetModeService: смена DNS адаптеров на 127.0.0.1 (dnscrypt) и восстановление при выходе/падении; список адаптеров, выбор «все/выбранные»; проверка занятости порта 53 | `nslookup ya.ru` уходит через dnscrypt; после выхода DNS возвращается к исходному | P0 |
| 3.2 | Системный прокси (WinINET/WinHTTP): вкл/выкл, исключения (bypass LAN), восстановление | `Invoke-WebRequest` в PowerShell идёт через прокси при включённой опции | P1 |
| 3.3 | Kill switch: `netsh advfirewall` правило «блокировать всё кроме доверенных exe» при падении модулей; автоснятие при корректном стопе | Убийство tor.exe → исходящий трафик заблокирован; Стоп → разблокирован | P1 |
| 3.4 | Elevation: explain-диалоги, перезапуск с правами админа только для привилегированных операций (или helper `runas`) | Не-админ режим: всё кроме 3.1–3.3 работает; админ: полный набор | P0 |
| 3.5 | Журнал привилегированных команд (аналог «лог root-команд») | Каждая netsh/registry операция логируется | P1 |

## Фаза 4 — Обвязка (≈3 ИЧД)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 4.1 | Автозапуск с задержкой (Task Scheduler/Run), настройка в «Общих» | После перезагрузки модули стартуют сами | P1 |
| 4.2 | Экран «Общие» настройки: язык, тема, автозапуск, DNS-режим, kill switch, звуки | Все опции из ТЗ §5.6 | P1 |
| 4.3 | Backup: экспорт/импорт zip (настройки + правила), экран бэкапа | Перенос настроек между машинами | P2 |
| 4.4 | UpdateService: проверка GitHub releases (API), уведомление, переход на страницу релиза | О new release приходит уведомление в tray | P2 |
| 4.5 | Help и About (версии/лицензии всех бинарников) | Экраны заполнены | P1 |

## Фаза 5 — DNS-правила (≈3 ИЧД)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 5.1 | Редакторы blacklist / whitelist / ip-blacklist / forwarding / cloaking (файлы, по правилу в строке, валидация) | Правило блокировки домена реально работает в резолве | P1 |
| 5.2 | Remote-списки: включение sources dnscrypt (public-resolvers/relays/odoh), интервал refresh | Списки обновляются, кэш в userData | P1 |
| 5.3 | Экран DNS Rules (список правил, счётчики, поиск) | UI-экран на шаблоне | P2 |

## Фаза 6 — Обход блокировок (≈2 ИЧД)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 6.1 | Включение obfs4proxy/snowflake/conjure при выборе мостов (ClientTransportPlugin exec), бандлинг их exe | Tor подключается через obfs4-мост | P0 (для заблокированных регионов) |
| 6.2 | Импорт мостов: вставка строк, парсер bridges.torproject.org, IPv6-мосты | Мосты добавляются из UI | P2 |

## Фаза 7 — TUN-режим, R&D (spike 3 ИЧД, затем отдельная оценка)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 7.1 | Spike: wintun.dll + tun2socks (или WinDivert-редирект) → весь TCP через SOCKS 9050 | PoC: браузер без настроек идёт через Tor | P2 |
| 7.2 | Решение go/no-go по итогам spike (стабильность, AV, лицензии wintun) | Меморандум + оценка или закрытие направления | P1 |

## Фаза 8 — Релиз (≈3 ИЧД)

| ID | Задача | DoD | Приоритет |
|---|---|---|---|
| 8.1 | Иконка/брендинг продукта, полное имя/версия | Нет упоминаний чужих товарных знаков в брендинге | P1 |
| 8.2 | Сборка NSIS + portable, тест на чистой Windows 10/11 (без Node) | Установка и работа без dev-окружения | P0 |
| 8.3 | Smoke/e2e: APP_SMOKE из шаблона + скрипт «старт всех модулей → проверить порты 9050/9053/7070 → стоп» в CI или локально | Скрипт зелёный на чистой машине | P1 |
| 8.4 | THIRD-PARTY-LICENSES: все лицензии + ссылки на исходники бинарников | Файл включён в дистрибутив и About | P0 (GPL) |
| 8.5 | README пользователя: установка, режимы, антивирус-исключения, ограничения (ТЗ §7) | README в дистрибутиве | P1 |

---

## Итого

- **MVP (фазы 0–3 + 2.7)**: ≈16 ИЧД — модули, конфиги, логи, DNS-режим, kill switch.
- **Паритет с Android (−перечисленные ограничения, +фазы 4–6)**: ≈24 ИЧД.
- **+TUN-режим (фаза 7)**: spike 3 ИЧД, далее отдельно.

## Правило работы с задачами

- Каждая задача = отдельная ветка/коммит(ы), DoD обязателен к закрытию.
- Задачи 1.x и 2.x параллелятся слабо (конфиги зависят от супервизора), 4.x–6.x — параллельно после MVP.
- Любое расхождение с ТЗ фиксировать прямо в ТЗ (раздел «Ограничения»), чтобы не копить молчаливые отличия.
